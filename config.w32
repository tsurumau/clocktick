// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("clocktick", "for clocktick support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("clocktick", "enable clocktick support", "no");

if (PHP_CLOCKTICK != "no") {
	EXTENSION("clocktick", "clocktick.c");
}

